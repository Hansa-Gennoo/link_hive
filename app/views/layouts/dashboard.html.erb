<!-- app/views/layouts/dashboard.html.erb -->
<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="dashboard">
    <header>
      <h1>Dashboard</h1>

      <div class="user-info">
        <%= current_user.username %>
      </div>

      <div class="side-bar">
        <nav>
          <%= link_to "Create Link", new_dashboard_landing_page_link_path(@landing_page) %> 
          <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete } %>
        </nav>

      </div>

      <div class="page-view">
        <%= link_to "View Page", landing_page_public_path(@landing_page) %>

      </div>
    </header>

    <main>
      <%#= yield %>
    </main>
  </body>
</html>
